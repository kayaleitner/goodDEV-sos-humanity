<flynt-component
    load:on="idle"
    name="BlockSeperatorLine"
    class="{{ options.mobileVisibility }} w-full h-0 max-w-screen max-w-screen-max mx-auto px-sm py-md md:p-md lg:p-lg"
    style="{% if options.colorText %} --textColor: {{ options.colorText }};{% endif %}"
    >
    <div class="border-b-1 border-textColor"></div>
</flynt-component>
