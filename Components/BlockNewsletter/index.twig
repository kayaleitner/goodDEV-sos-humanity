<flynt-component client:visible name="BlockNewsletter" style="background-color: {% if options.colorBackground %}{{ options.colorBackground }}{% else %}var(--grey){% endif %};">
  <div class="max-w-screen-max mx-auto px-sm py-lg lg:px-lg lg:py-lg flex flex-col lg:flex-row justify-between items-stretch gap-lg">
    <div class="w-full lg:w-1/2 not-prose  ">{{ contentHtml|e('wp_kses_post') }}</div>
    <div class="w-full lg:w-1/2 flex items-center justify-end">{{ contactForm }}</div>
  </div>
</flynt-component>
