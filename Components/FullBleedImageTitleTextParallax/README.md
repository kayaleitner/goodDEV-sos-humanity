# Hero Image Title Text Parallax

A full width image with a set aspect ratio with a separate mobile image upload to optimize the crop. A Title on the top. Formatted text in a yellow box on the bottom. Linebreaks (br-tags) convert to spaces on mobile for individual text flow control on desktop.

