{% import "Components/_Atoms/Button/macro.twig" as btn %}

{{ btn.buttonAnchor({ url: ctaLink.url, title: ctaLink.title }, {
  color: 'accent',
  style: navOnLight ? '' : 'outline',
  icon: true,
}) }}
